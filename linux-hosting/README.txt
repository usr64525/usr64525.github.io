These are configuration files suitable for e.g. a VirtualBox server run from a Linux/Windows/macOS host.
It is assumed that the VM has 2 VHDs, for example, one ten-gigabyte "main" and another hundred-gigabyte VHD for the webserver.
Debian, 64-bit, 2 CPUs, 2 GB RAM, 2 bridged NetworkInterfaceCards.
